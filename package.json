{
  "name": "nacademy",
  "version": "1.0.0",
  "private": true,
  "description": "nAcademy — multi-branch sports academy management (static site: vanilla JS + Supabase, offline-capable PWA).",
  "scripts": {
    "build": "echo \"Static site — no build step needed.\"",
    "start": "npx serve .",
    "format": "prettier --write \"*.{js,css,html,json}\"",
    "check": "node --check main.js && node --check server.supabase.js && node --check sw.js",
    "test": "node --test tests/unit.test.js"
  },
  "devDependencies": {
    "prettier": "^3.9.4"
  }
}
